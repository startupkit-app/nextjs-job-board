# ── Required ─────────────────────────────────────────────────────────────────

# Secret API key (sk_…) — Kit → Hiring → Career Portal → Public API Keys.
# Used ONLY on the server (Server Components + Server Actions). Never exposed
# to the browser.
STARTUPKIT_SECRET_KEY=

# ── Optional ─────────────────────────────────────────────────────────────────

# Kit API base URL. Only change this if you run Kit on a custom domain.
STARTUPKIT_BASE_URL=https://app.startupkit.app

# Publishable API key (pk_…) from the same Public API Keys page. Safe to expose:
# it only lets the visitor's browser report job analytics (views, traffic
# sources, view → apply funnel) to Kit → Hiring → Analytics. Leave it empty and
# analytics are simply off. If the key has an origin allowlist, add this site's
# origin to it.
NEXT_PUBLIC_STARTUPKIT_PUBLISHABLE_KEY=

# Browser-side counterpart of STARTUPKIT_BASE_URL. Only set it alongside a
# custom STARTUPKIT_BASE_URL.
NEXT_PUBLIC_STARTUPKIT_BASE_URL=

# Cloudflare Turnstile site key. If set, the apply form renders the Turnstile
# widget and forwards its token with each submission. Required when the job's
# application form has turnstile.required = true.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# Signing secret of the Kit webhook endpoint that points at POST /api/revalidate.
# If set, job lifecycle events bust the cache instantly instead of waiting for
# ISR revalidation. Requests are authenticated by verifying Kit's HMAC-SHA256
# signature, so this must be the endpoint's own signing secret, not a secret of
# your choosing. Kit generates it when the webhook endpoint is created.
KIT_WEBHOOK_SIGNING_SECRET=

# Company name shown in the header, page titles, and schema.org JobPosting
# structured data.
NEXT_PUBLIC_COMPANY_NAME=
