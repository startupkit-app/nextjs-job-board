import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Intentionally minimal — fork away. The template has no required Next.js
  // configuration beyond the defaults.
};

export default nextConfig;
